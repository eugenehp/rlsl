name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  RUSTFLAGS: "-D warnings"

jobs:
  # ── Formatting & linting ──────────────────────────────────────────
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Install mold
        run: sudo apt-get update && sudo apt-get install -y mold
      - name: Enable mold
        run: echo 'RUSTFLAGS=-D warnings -C link-arg=-fuse-ld=mold' >> "$GITHUB_ENV"
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Clippy (workspace, exclude rlsl-py/rlsl-wasm)
        run: >
          cargo clippy
          -p rlsl -p rlsl-sys -p exg -p rlsl-rec -p rlsl-rec-gui -p rlsl-cli
          -- -D warnings

  # ── Dependency audit ──────────────────────────────────────────────
  deny:
    name: cargo-deny
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: EmbarkStudios/cargo-deny-action@v2
        with:
          command: check advisories licenses bans sources

  # ── Build & test on all platforms ─────────────────────────────────
  test:
    name: Test (${{ matrix.os }})
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: macos-latest
            target: aarch64-apple-darwin
          - os: windows-latest
            target: x86_64-pc-windows-msvc
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
        with:
          prefix-key: "v1"

      # mold is Linux-only
      - name: Install mold
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y mold
      - name: Enable mold
        if: runner.os == 'Linux'
        run: echo 'RUSTFLAGS=-D warnings -C link-arg=-fuse-ld=mold' >> "$GITHUB_ENV"

      # Disable debug info on Windows to avoid PDB disk space exhaustion
      - name: Disable debuginfo (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          "[profile.dev]`ndebuginfo = 0`n[profile.test]`ndebuginfo = 0" | Out-File -Append -Encoding utf8 Cargo.toml
          if (Test-Path target) { cargo clean }

      - name: Build workspace
        run: >
          cargo build
          -p rlsl -p rlsl-sys -p exg -p rlsl-rec -p rlsl-cli
          --target ${{ matrix.target }}

      # GUI needs extra deps on Linux
      - name: Install Linux GUI deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get install -y libxkbcommon-dev libwayland-dev \
            libxcb-render0-dev libxcb-shape0-dev libxcb-xfixes0-dev \
            libvulkan-dev

      - name: Build GUI
        run: cargo build -p rlsl-rec-gui --target ${{ matrix.target }}

      - name: Build bridge
        run: >
          cargo build -p rlsl-wasm --features bridge --bin rlsl-bridge
          --target ${{ matrix.target }}

      - name: Unit tests (rlsl)
        run: cargo test -p rlsl --target ${{ matrix.target }}

      - name: E2E tests (rlsl-rec)
        if: runner.os == 'Linux'
        run: cargo test -p rlsl-rec --target ${{ matrix.target }} -- --nocapture

      - name: Check liblsl symbols
        shell: bash
        run: |
          if [ "$RUNNER_OS" = "macOS" ]; then
            nm -gU target/${{ matrix.target }}/debug/liblsl.dylib | grep ' T _lsl_' | wc -l
          elif [ "$RUNNER_OS" = "Linux" ]; then
            nm -D target/${{ matrix.target }}/debug/liblsl.so | grep ' T lsl_' | wc -l
          else
            echo "(symbol check skipped on Windows)"
          fi

  # ── Benchmarks (compile check only) ──────────────────────────────
  bench:
    name: Benchmarks
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install mold
        run: sudo apt-get update && sudo apt-get install -y mold
      - name: Enable mold
        run: echo 'RUSTFLAGS=-D warnings -C link-arg=-fuse-ld=mold' >> "$GITHUB_ENV"
      - name: Check benchmarks compile
        run: cargo bench --no-run

  # ── Documentation ─────────────────────────────────────────────────
  docs:
    name: Rustdoc
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install mold
        run: sudo apt-get update && sudo apt-get install -y mold
      - name: Enable mold
        run: echo 'RUSTFLAGS=-D warnings -C link-arg=-fuse-ld=mold' >> "$GITHUB_ENV"
      - name: Build docs
        run: cargo doc --workspace --no-deps --exclude rlsl-py --exclude rlsl-wasm
        env:
          RUSTDOCFLAGS: "-D warnings"

  # ── WASM build ────────────────────────────────────────────────────
  wasm:
    name: WASM build
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
      - uses: Swatinem/rust-cache@v2
      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh
      - name: Build WASM package
        working-directory: crates/rlsl-wasm
        run: wasm-pack build --target web --features wasm --no-default-features

  # ── Python wheels ─────────────────────────────────────────────────
  python:
    name: Python (${{ matrix.os }})
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64
          - os: macos-latest
            target: aarch64
          - os: windows-latest
            target: x64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Build wheel
        uses: PyO3/maturin-action@v1
        with:
          command: build
          args: --release -m crates/rlsl-py/Cargo.toml --out dist
      - name: Upload wheel
        uses: actions/upload-artifact@v4
        with:
          name: python-wheel-${{ matrix.os }}
          path: dist/*.whl

  # ── Docker build ──────────────────────────────────────────────────
  docker:
    name: Docker
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build -t rlsl:ci .
      - name: Verify image
        run: docker run --rm rlsl:ci version
